<% @page_title = "Chronology" %>

<%= link_to("<< Back to Index", root_path) %>

<div class="tracks index">
  <h1>The Tracks Chronologically</h1>

  <p>&nbsp;</p>

  <table class="listing" summary="Track list"

    <tr class="header">
      <th>Track</th>
      <th>Date finished</th>
    </tr>

    <% @tracks.each do |track| %>
    <tr>
      <td><%= track.name %></td>
      <td><%= track.date_finished %></td>

      </td>
    </tr>
  <% end %>
  </table>

</div>
