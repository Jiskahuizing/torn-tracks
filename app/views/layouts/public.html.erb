<!DOCTYPE html>
<html>

  <head>
    <title>Torn Tracks</title>
    <%= stylesheet_link_tag('application', :media => 'all') %>
    <%= javascript_include_tag('application') %>
    <script> document.addEventListener('play', function(e){
        var audios = document.getElementsByTagName('audio');
        for(var i = 0, len = audios.length; i < len;i++){
            if(audios[i] != e.target){
                audios[i].pause();
              }}},
        true); </script>
  </head>

  <body>

      <div id="main">
        <div id ="navigation">
          <%= render(:partial => 'layouts/navigation', :locals => { :tracks => @tracks}) %>
        </div>

        <div id="content">
          <%= yield %>
        </div>

      </div>
      <div id="footer">
        <p id="copyright">&copy; Jiska Huizing 2017</p>
      </div>

  </body>

</html>
